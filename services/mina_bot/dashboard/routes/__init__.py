"""
Dashboard route modules (Batch-20 placeholder).

Batch-20 split-routing was reverted for stability. These modules are kept only
for compatibility, but are not used by the current dashboard.app.
"""
