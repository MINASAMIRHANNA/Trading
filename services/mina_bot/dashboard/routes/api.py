"""
API routes placeholder.

The dashboard currently uses the monolithic `dashboard.app` (stable).
This file intentionally contains no routes.
"""

from fastapi import APIRouter

router = APIRouter()
